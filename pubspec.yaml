name: weather_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  # ui handling
  flutter_screenutil: ^5.5.3+2
  # state management tools
  flutter_bloc: ^8.1.1
  # api handling
  dio: ^4.0.6
  # object comparing package
  equatable: ^2.0.5
  # date-time with formatting
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/sun.gif
    - assets/images/moon.gif
